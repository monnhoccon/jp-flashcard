class RepoInfo {
  String title;
  List<String> tagList;
  int numTotal;
  int numMemorized;
  int repoId;
  RepoInfo(
      {this.title, this.tagList, this.numTotal, this.numMemorized, this.repoId});
}
